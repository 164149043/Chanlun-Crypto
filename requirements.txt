# 缠论量化分析框架依赖
# Vercel 部署精简版 - 只包含必需依赖

# ============================================
# 核心依赖（缠论计算必需）
# ============================================
numpy>=1.23.0
pandas>=1.4.0
requests>=2.28.0

# ============================================
# Web 后端依赖（API 必需）
# ============================================
fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.27.0
pydantic>=2.0.0
mangum>=0.17.0  # Vercel ASGI 适配器

# ============================================
# 以下依赖仅本地开发使用，Vercel 不需要
# 如需使用请创建 requirements-dev.txt
# ============================================
# matplotlib>=3.5.0  # 数据可视化
# ipython>=8.5.0     # 开发工具
